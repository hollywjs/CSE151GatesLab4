
public class Clock extends Gate {
	
	/**
	 * This is used to construct a Clock, sets the image and calls the
	 * setIcon method from the parent class, resizes image.
	 */
	public Clock(){
		myIcon = "src/CLOCKSpeed1.png";
		setIcon();
	}

}
